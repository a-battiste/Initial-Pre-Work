// I learned that arguments can be passed with arrow functions, and that more than 1 argument can be passed with an arrow function.

const myConcat = (arr1, arr2) => arr1.concat(arr2);

// test your code
console.log(myConcat([1, 2], [3, 4, 5]));
