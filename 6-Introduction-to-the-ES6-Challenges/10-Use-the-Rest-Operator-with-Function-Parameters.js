// I learned that the rest operator is used to create a more dynamic function which can be used for a various amount of arguments.

const sum = (function() {
  "use strict";
  return function sum(...args) {
    return args.reduce((a, b) => a + b, 0);
  };
})();
console.log(sum(1, 2, 3)); // 6
