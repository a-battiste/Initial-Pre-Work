// I learned that that you don't always have to name your functions, especially when the function just passed as an argument to another function. Arrow functions can simplify writing these functions by using =>. Arrow functions also serve as a way to return a return value without using the return keyword.

const magic = () => new Date()
