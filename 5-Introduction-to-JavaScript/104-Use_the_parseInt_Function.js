// I learned that the parseInt function converts a string into an integer. Also, if the first character in the string cannot be converted, it returns NaN.

function convertToInteger(str) {
  return parseInt(str);
}

convertToInteger("56");
