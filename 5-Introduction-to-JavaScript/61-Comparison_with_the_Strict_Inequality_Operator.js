// I learned that the strict inequality operator checks for inequality without converting the data types of each variable. This operator gives the opposite return of the the strict equality operator. 

// Setup
function testStrictNotEqual(val) {
  // Only Change Code Below this Line

  if (val !== 17) {

  // Only Change Code Above this Line

    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testStrictNotEqual(10);
