// I learned that the Math.random() function creates a random number equal to or greater than 0 but less than 1. 

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
