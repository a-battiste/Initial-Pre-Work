// I learned how to use the equality operator to create a Boolean statement. The == operator can compare different data types, but converts one type to the other which is called type coercion.

// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);
