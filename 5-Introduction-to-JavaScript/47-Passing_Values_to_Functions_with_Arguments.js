// I learned that functions use parameters that serve as placeholders for values that will be input later, which are called arguments. 

// Example
function ourFunctionWithArgs(a, b) {
  console.log(a - b);
}
ourFunctionWithArgs(10, 5); // Outputs 5

// Only change code below this line.

function functionWithArgs(a,b) {
  console.log(a + b);
}
functionWithArgs(100, 50);
