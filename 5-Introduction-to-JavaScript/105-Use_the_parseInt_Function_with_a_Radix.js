// I learned that the radix is the base of a number, which specifies the base of the string converted with the parseInt function.

function convertToInteger(str, a) {
  return parseInt(str, 2);
}

convertToInteger("10011");
